# Libreria prueba-lib-npm

Mi primera libreria con npm que proporciona componentes personalizables de texto y botón para tus aplicaciones React.

## Instalación

Para instalar la librería, ejecuta:

npm install prueba-lib-npm
